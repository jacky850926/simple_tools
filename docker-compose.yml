version: '3'

services:
  web:
    container_name: simple_tools_v1
    image: simple_tools:v1
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    ports:
      - "3002:3000"
    environment:
      - NODE_ENV=production
